
define(["../../js/modules/Helpers"], function (Helpers) {

	describe("Helpers", function () {
		it("should return an object", function () {
			expect(typeof Helpers === 'object').toBe(true);
		});
	});

});